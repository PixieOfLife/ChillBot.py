version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.11"
  jobs:
    post_install:
      - pip install poetry==1.4.2 sphinx
      - poetry config virtualenvs.create false
      - pip install sphinx-rtd-theme==1.0.0

sphinx:
  fail_on_warning: false